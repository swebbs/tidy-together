{
  "name": "chore-tracker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.sw.json && vite build",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "preview": "vite preview"
  },
  "dependencies": {
    "firebase": "^12.1.0",
    "vite": "^7.1.2"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
